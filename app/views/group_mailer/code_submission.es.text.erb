Hola <%= @group.name %>,

Tu código para acceder a la plataforma es <%= @group.auth_token %>. Puedes iniciar sesión en <%= root_url %>. Este código será válido hasta el <%= l(@group.auth_token_expires_at, format: :long) %>.

Los votos disponibles en los distintos órganos de decision son:

<%= plain_mail_vote_distribution(@group) %>

En caso de que hubiera algún problema con la distribución de los votos, por favor contacta a la organización.