Hola <%= @group.name %>,

Tu código para acceder a la plataforma es <%= @group.auth_token %>. Puedes iniciar sesión en <%= root_url %>.

Este código será válido hasta el <%= l(@group.auth_token_expires_at, format: :long) %>.

