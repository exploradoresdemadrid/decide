Hello <%= @group.name %>,

Your code to access the platform is <%= @group.auth_token %>. You can use this code to log in at <%= root_url %>. This code will be valid until <%= l(@group.auth_token_expires_at, format: :long) %>.

The votes available for the different decision-making bodies are:

<%= plain_mail_vote_distribution(@group) %>

Should there be any problem with the distribution of the votes, please contact the organization.