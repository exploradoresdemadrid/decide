<h1>Editing Voting</h1>

<%= render 'form', voting: @voting %>

<%= link_to t('show'), organization_voting_path(@organization, @voting) %> |
<%= link_to t('back'), organization_votings_path(@organization) %>
