<h1><%= t('votings')%></h1>

<%= votings_table(@organization, @non_archived_votings) %>

<h2 data-toggle="collapse" href="#collapseExample">Votaciones archivadas</h2>
<div id="collapseExample" class="collapse">
  <%= votings_table(@organization, @archived_votings) %>
</div>

<%= alert_box t('no_votings') if @votings.none? %>
<br>

<%= link_to t('new_voting'), new_organization_voting_path(@organization) if can?(:create, Voting) %>