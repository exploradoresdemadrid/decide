<%= simple_form_for([@voting, @question]) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :voting_id, as: :hidden, input_html: { value: @voting.id } %>
  <%= f.button :submit %>
<% end %>