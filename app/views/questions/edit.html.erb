<h1> <%= t('edit_questions') %></h1>

<%= render 'form', question: @question %>

<%= link_to t('show'), organization_voting_question_path(@organization, @voting, @question) %> |
<%= link_to t('back'), organization_voting_questions_path(@organization, @voting) %>
